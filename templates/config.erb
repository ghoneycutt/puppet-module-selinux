# This file is being maintained by Puppet.
# DO NOT EDIT

# This file controls the state of SELinux on the system.
# SELINUX= can take one of these three values:
# enforcing - SELinux security policy is enforced.
# permissive - SELinux prints warnings instead of enforcing.
# disabled - SELinux is fully disabled.
SELINUX=<%= @mode %>
# SELINUXTYPE= type of policy in use. Possible values are:
# targeted - Only targeted network daemons are protected.
# strict - Full SELinux protection.
SELINUXTYPE=<%= @type %>
<% if @setlocaldefs -%>
# SETLOCALDEFS= Controls how local definitions (users and booleans) are set.
# Possible values are:
# 0 - have them controlled by semanage.
# 1 - have these definitions controlled by load_policy from files in
# /etc/selinux/<policyname>.
SETLOCALDEFS=<%= @setlocaldefs %>
<% end -%>
